version: '3.8'

services:
  calculator:
    build: .
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./:/usr/share/nginx/html
    environment:
      - TZ=UTC
    restart: unless-stopped
